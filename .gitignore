# Sensitive deployment config — copy to apprunner-service.json and fill in your values
apprunner-service.json

# Claude Code local settings
.claude/

# OS
.DS_Store
Thumbs.db
test_results.txt

# Eval run results (keep baseline.json if you want to commit it)
tests/results/run_*.json

# Local notes / future improvement ideas
future_improvements.md

# SQLite visitor database (runtime data, not source)
Backend/data/
